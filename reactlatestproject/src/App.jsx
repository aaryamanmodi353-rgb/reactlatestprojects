import Mainclicks from "./Impt";


const obj = " one element so we can use either div or fragments this is for vitelatest only function name should be capital jsx can only be used "
function App() {
  
  

  return (

    // we just need to use the {} for represent
<>
    <h1>we can return only add {obj}</h1>

    <Mainclicks/>
    
    </>
  )
}

export default App
